# Handoff

The h5ad/QC handoff packet at
`/mnt/pikachu/JAX_scRNAseq02_handoff/JAX-scRNAseq02-5-22-26` included
`PROVENANCE.md`. That packet provenance is preserved as
`temporary_packet_provenance.md`; this run record is now canonical.

Destination root:

```text
/JAX_scRNAseq02_processed/JAX-scRNAseq02-5-22-26
```

The packet-level Globus submission response recorded task
`efe36214-55e5-11f1-9243-02c81e62ebf9`. The source packet listed 170 payload
files and 41,251,425,950 payload bytes. The packet did not include a final
Globus status JSON capture.

Per-library large-file Globus task JSON/inventory captures from the production
root are preserved as `25E*.globus_large_files_*` files when present.
