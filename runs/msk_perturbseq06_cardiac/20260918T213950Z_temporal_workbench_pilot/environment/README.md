# Environment

Exact repository commits and runtime checks are recorded in `run.json` and
`logs/runtime-preflight.log`. The web service uses the dedicated Python
environment `/mnt/pikachu/.venvs/bwb-workbench-staged`, including FastAPI
0.115.8 and Starlette 0.45.3.
