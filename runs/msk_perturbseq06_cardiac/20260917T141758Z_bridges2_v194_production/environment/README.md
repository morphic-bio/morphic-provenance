# Environment

Bridges-2 `RM-shared`; 64 allocated CPUs and 120 GB per production capture.
STAR Suite v1.9.4 commit `1c9ddb9`, portable `WITH_CHROMAP=0` build. See the
preceding pilot record for the complete build manifest and two-host acceptance.
